# Chiave API v3 di TMDb — gratuita: https://www.themoviedb.org/settings/api
TMDB_API_KEY=

# Segreto per firmare i cookie di sessione — genera con: openssl rand -hex 32
SESSION_SECRET=

# Percorso del database SQLite (default ./data/serate.db)
# DATABASE_PATH=./data/serate.db
